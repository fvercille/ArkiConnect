[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = ["php -r \"echo phpinfo();\" | grep mysqli"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t /app"